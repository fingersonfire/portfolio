name: portfolio
description: A portfolio website written in Flutter.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.3
  get: ^4.2.4
  url_launcher: ^6.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - lib/assets/profile.png
    - lib/assets/codeImage.jpg
    - lib/assets/github.png
    - lib/assets/linkedin.png